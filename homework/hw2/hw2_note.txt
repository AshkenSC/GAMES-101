1. 创建三角形的bounding box
用什么算法创建box
思路：计算三个顶点之间，x坐标差的最大值，记录是哪两点之间x差最大；
计算三个顶点之间，y坐标差的最大值，记录是哪2点。

如何表示bounding box？
答：不表示，只是找到坐标的范围，然后用循环遍历这范围内的所有点，一个个地用insideTriangle判断

2. 遍历box内所有像素。然后，使用像素中心的屏幕空间坐标来检查中心点是否在三角形内。 
ok如何获取box的像素数组？
答：三角形三个顶点横纵坐标最大值
ok如何获取像素中心的屏幕空间坐标？

3. 如果在内部，则将其位置处的插值深度值 (interpolated depth value) 与深度缓冲区 (depth buffer) 中的相应值进行比较。 
depth buffer放在了哪里？函数没有传入这个参数，也没找到一个全局变量
答：找到了，在rasterizer.hpp里，定义为depth_buf。

接下来的问题就是如何访问到depth buffer里面(x, y)的位置。这是一个一维数组，所以需要做一些计算。
发现buffer是w*h的，但这不够，因为你不知道是先存一行，还是先存一列
又发现有一个函数get_index可以直接根据坐标x，y获取位置